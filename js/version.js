// ---------------------------------------------------------------------------
// Bump this whenever you deploy something you need to confirm actually
// reached a device. It's shown at the bottom of Settings, so "is my phone
// running the current code or a cached copy?" is answerable in two taps
// instead of by guesswork.
// ---------------------------------------------------------------------------

export const BUILD_VERSION = "2026-09-22.5";
